import { types } from "../types/types"

// accion que permite al usuario autenticarse
export const login =  {
    type: types.login
    }
;  
// accion que permite al usuario desautenticarse
export const logout =  { 
    type: types.logout
    
    
    
};
